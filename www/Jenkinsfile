pipeline {
    agent any
    stages {
        stage("Composer") {
            steps {
                bat 'cd www && composer install'
            }
        }
        stage("W podkatalogu www uruchomienie PHP bez autoloadera") {
            steps {
                bat 'cd www && dir && php skrypt-bez-autoload.php'
            }
        }
    }
}